#include <bits/stdc++.h>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int n, p;
    cin >> n >> p;
    vector<int> L(n);
    vector<vector<int>> P(p);
    for(int i = 0; i < n; i++)
	cin >> V[i];
    
}

